import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: '<app-carousel></app-carousel>',
  styles: [`
    import
  `]
})
export class AppComponent {
  title = 'app';
}
